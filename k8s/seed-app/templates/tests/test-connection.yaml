apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seed-app.fullname" . }}-test-connection"
  labels:
    {{- include "seed-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "seed-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
